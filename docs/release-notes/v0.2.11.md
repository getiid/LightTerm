## 本次更新（代码片段同步 + 模块命名整理）

### 同步修复
- 代码片段从浏览器本地存储迁移到共享数据库，和主机、密钥一样可随数据库一起同步。
- 首次启动新版本时，会自动迁移旧版本地代码片段到数据库。

### 界面整理
- 统一侧边栏和页面标题命名：
  - 主机管理
  - 文件传输
  - 代码片段
  - 串口工具
  - 密钥管理
  - 应用设置

### 说明
- 若另一台设备仍是旧版，代码片段仍不会跟随同步；请两端都升级到本版本或更高版本。
