# AstraShell 开发交接（2026-03-12）

## 1. 当前项目结构
- 桌面端主工程：`/Users/jonny/Desktop/AstraShell/app`
- 前端主页面：`/Users/jonny/Desktop/AstraShell/app/src/App.vue`
- Electron 主进程：`/Users/jonny/Desktop/AstraShell/app/electron/main.mjs`
- 预加载桥接：`/Users/jonny/Desktop/AstraShell/app/electron/preload.mjs`

## 2. 今天已完成的核心功能
- 主机页三栏布局：左分类、中主机卡片、右编辑。
- SFTP 双栏文件管理：左右独立链接、目录浏览、拖拽上传下载、排序（A-Z/创建/修改）。
- 密钥仓库：公钥/私钥/证书改为“至少填写一项”，卡片式列表+右侧编辑。
- 新增“代码片段”模块：
  - 支持分类、搜索、卡片列表、右侧编辑。
  - 支持绑定主机或复用当前 SSH 会话。
  - 支持“一键逐条执行命令”、执行间隔、停止执行。
  - 本地持久化存储 key：`astrashell.snippets.v1`。
  - 内置示例片段：`部署 Docker（Debian/Ubuntu）`。

## 3. 构建与打包
- 前端构建：`cd app && npm run build`
- 桌面打包（默认脚本）：`cd app && npm run dist`
- 由于 macOS `codesign` 与扩展属性冲突，已加入打包钩子：
  - 文件：`app/electron/after-pack.cjs`
  - 配置：`app/package.json` 的 `build.afterPack`
- 本次成功产物（独立安装包）：
  - `app/release/AstraShell-0.2.0-arm64.dmg`

## 4. 下次继续开发的建议入口
1. 先运行 `cd app && npm run build` 确认环境正常。
2. 启动开发：`cd app && npm run dev`。
3. 优先继续模块：
   - 代码片段增加“执行日志视图”和“步骤失败中断策略”。
   - 代码片段持久化从 localStorage 升级到 SQLite（跨端更稳）。
   - 主机/密钥/片段三者联动（统一标签、批量操作）。

## 5. 已知事项
- `package.json` 里缺 `description/author`，不影响运行，但打包会有提示。
- 现在 DMG 为可用构建；如需正式分发签名公证，后续要接入 Apple Developer 证书链。
